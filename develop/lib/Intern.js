const Employee = require('./Employee');

class Intern extends Employee {
    constructor(school){
        super(inernName, internId, internEmail)
        this.school = school;
    }
    getSchool(){
        return School
    }

    getRole(){
        return Intern
    }
}

const school = 'schoolname'

module.exports = Intern;